#include<bits/stdc++.h>
using namespace std;

int main(){
    int n,m;
    cin>>n>>m;
    int max=0,min=0;
    vector<int> v;
    for(int i=0;i<m;i++)
    {int data;cin>>data;
    v.emplace_back(data);}
    sort(v.begin(),v.end(),greater<int>());

    
    
    while
}